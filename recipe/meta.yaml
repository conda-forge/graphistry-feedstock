{% set name = "graphistry" %}
{% set version = "0.29.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/graphistry-{{ version }}.tar.gz
  sha256: d766b2760f7e3ceb3acaa4f7bb56e5e815dd7d5f0c86b8910d926a8604c1d0fa

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.7
    - setuptools <60.0.0
  run:
    - numpy
    - packaging >=20.1
    - palettable >=3.0
    - pandas <2.0.0
    - pyarrow >=0.15.0
    - python >=3.7
    - requests
    - setuptools <60.0.0
    - squarify
    - typing-extensions

test:
  imports:
    - graphistry
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/graphistry/pygraphistry
  summary: A visual graph analytics library.
  doc_url: https://pythonhosted.org/graphistry/
  license: BSD-3-Clause
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - thewchan
